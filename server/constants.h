#ifndef CONSTANTS_H
#define CONSTATNS_H

/** Squares per second. */
const double MOVE_SPEED = 1.0;

/** Radians per second. */
const double TURN_SPEED = 1.0;

const double PLAYER_RADIUS = 0.5;

#endif
